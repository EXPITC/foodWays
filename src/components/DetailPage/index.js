import { React, useEffect, useStatus } from `react`;
import Header from `../Header`
import { Wrapper, } from `./DetailPage.styled`
const DetailPage = () => {

    return (
        <>
            <Header/>
            <Wrapper>
                Hello word
            </Wrapper>
        </>
    )
}

export default DetailPage