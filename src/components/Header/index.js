import { React, useEffect, useStatus } from `react`;

import { Head } from `./Header.styled`

const Header = () => {

    return (
        <Head>
            Hello head
        </Head>
    )
}

export default Header